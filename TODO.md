These are short-term goals, not long-term project visions. For that, see SPEC.md. 

- Query parsing
- Single argument queries (where(:foo => :bar), but not where(:foo => :bar, :fizz => :buzz))
- Redis integration